#ifndef LDP_LOGGER_HPP
#define LDP_LOGGER_HPP

#include <ldp/core/logger.hpp>

namespace ldp
{
    class Logger : public ldp::Logger
    {
    public:
        Logger() = default;
        ~Logger() = default;
        
    };
}

#endif // LDP_LOGGER_HPP